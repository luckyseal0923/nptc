import {rpc} from '@/lib/supabase';
'use client';
import {useEffect,useState} from 'react';
import {STATIONS,formatScore,gradeStatus,type Grade,type Threshold} from '@/lib/grading';
import {NativeSelect,NativeSelectOption} from '@/components/ui/native-select';
import {Button} from '@/components/ui/button';
type RecordItem={id:string;name:string;code:string;workshopName:string;published:number;updatedAt:string|null;grades:(Grade&{key:string})[];thresholds:Threshold[]};
export default function StudentDashboard({isTeacher}:{isTeacher:boolean}){
 const [records,setRecords]=useState<RecordItem[]|null>(null),[selected,setSelected]=useState(''),[error,setError]=useState('');
 useEffect(()=>{const abort=new AbortController();rpc<{records:RecordItem[]}>('nptc_student_data',{},abort.signal).then(data=>{setRecords(data.records);}).catch(e=>{if(e.name!=='AbortError')setError(e.message);});return()=>abort.abort();},[]);
 const record=records?.find(r=>r.id===selected)??records?.[0];
 return <><div className="dashboard-title"><div><h1>我的 OSCE 成績</h1><p>{record?`${record.name}，回顧每一次練習，整理下一步的方向。`:'回顧每一次練習，整理下一步的方向。'}</p></div>{isTeacher&&<a className="text-link" href="/teacher">前往老師專區 ↗</a>}</div>{error?<section className="notice error" role="alert"><p>{error}</p><Button className="action" onClick={()=>location.reload()}>重新載入</Button></section>:!records?<p className="empty-panel" role="status">正在載入你的學習紀錄…</p>:!record?<section className="empty-panel"><span className="section-label">YOUR RECORDS</span><h2>目前尚無工作坊紀錄</h2><p>請確認登入 Email 與老師建立的名冊一致。若已參加工作坊，請洽課程老師協助確認。</p>{isTeacher&&<a className="button" href="/teacher">進入老師專區 ↗</a>}</section>:<><div className="workshop-toolbar"><label>工作坊梯次<NativeSelect value={record.id} onChange={e=>setSelected(e.target.value)}>{records.map(r=><NativeSelectOption key={r.id} value={r.id}>{r.workshopName}</NativeSelectOption>)}</NativeSelect></label><span className="identity-pill">學號 {record.code}</span></div>{!record.published?<section className="empty-panel"><h2>本梯次成績尚未公布</h2><p>老師確認成績後，將於此頁開放查詢，請留意課程通知。</p></section>:<><div className="student-summary"><div><span>評量紀錄</span><strong>{record.grades.filter(g=>g.score!==null).length}<small> / 4 題已登錄</small></strong></div><div><span>每題固定及格線</span><strong>60<small> / 100</small></strong></div><p>及格狀態以固定 60 分判定。<br/>邊緣及格分數為各題 Rating＝3 學員的平均分數，另列供參考。</p></div>{[1,2].map(day=><section className="day-section" key={day}><div className="day-heading"><span>DAY {String(day).padStart(2,'0')}</span><h2>第{day===1?'一':'二'}天 OSCE</h2></div><div className="student-score-grid">{STATIONS.filter(s=>s.day===day).map(s=>{const g=record.grades.find(g=>g.key===s.key),score=g?.score??null,t=record.thresholds.find(t=>t.key===s.key);return <article className="student-score-card" key={s.key}><div className="panel-heading"><h3>{s.title}</h3><span className={`result-badge ${score===null?'pending':score>=60?'pass':'below'}`}>{gradeStatus(score)}</span></div><div className="personal-score">{formatScore(score)}<span> / 100</span></div><div className="score-track" aria-label={`個人成績 ${formatScore(score)} 分，及格線 60 分，邊緣及格分數 ${formatScore(t?.value??null)}`}><div className="score-fill" style={{width:`${score??0}%`}}/><i className="pass-marker" style={{left:'60%'}}/>{t?.value!==null&&t?.value!==undefined&&<i className="border-marker" style={{left:`${t.value}%`}}/>}</div><div className="track-labels"><span>0</span><span>100</span></div><dl className="grade-details"><div><dt>固定及格分數</dt><dd>60 分</dd></div><div><dt>邊緣及格分數</dt><dd>{t?.value===null||t?.value===undefined?'尚無法計算':`${formatScore(t.value)} 分`}</dd></div><div><dt>Global Rating</dt><dd>{g?.rating??'—'} / 5</dd></div></dl><p className="score-note">{t?.count?`邊緣分數採 ${t.count} 位 Rating＝3 學員的該題分數平均。`:'本題尚無 Rating＝3 且已登錄分數的資料。'}</p></article>})}</div></section>)}<div className="score-legend"><span><i className="legend-fixed"/>固定及格線 60 分</span><span><i className="legend-border"/>邊緣及格線</span></div><p className="portal-note">成績更新：{record.updatedAt?new Intl.DateTimeFormat('zh-TW',{dateStyle:'medium',timeStyle:'short',timeZone:'Asia/Taipei'}).format(new Date(record.updatedAt)):'—'}。本頁為個人工作坊測驗紀錄，並非國家考試成績。顯示數值最多取至小數點後兩位。</p></>}</>}</>;
}

